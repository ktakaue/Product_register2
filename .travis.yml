sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker-compose exec --env 'RAILS_ENV=test' web db:create  bash
  - docker-compose exec --env 'RAILS_ENV=test' web db:migrate  bash
  - docker-compose exec --env 'RAILS_ENV=test' web db:test  bash
